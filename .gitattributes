# Git-crypt encryption configuration
# Files matching these patterns will be encrypted in the repository

# Environment files with secrets
.env.local filter=git-crypt diff=git-crypt
.env.production filter=git-crypt diff=git-crypt
.env.staging filter=git-crypt diff=git-crypt

# Any other sensitive configuration files
*.secret filter=git-crypt diff=git-crypt
**/secrets/* filter=git-crypt diff=git-crypt

# Zitadel service account key files
339916934214621265.json filter=git-crypt diff=git-crypt
*-service-account.json filter=git-crypt diff=git-crypt

# Note: These files will appear encrypted in the repository
# but will be transparently decrypted for users with the git-crypt key