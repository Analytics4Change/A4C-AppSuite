{
  "name": "@a4c/event-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Event schema contracts and code generation",
  "scripts": {
    "bundle": "cd asyncapi && asyncapi bundle asyncapi.yaml -o ../asyncapi-bundled.yaml",
    "validate": "asyncapi validate asyncapi/asyncapi.yaml",
    "replace-enums": "node scripts/replace-inline-enums.js",
    "generate:types": "npm run replace-enums && npm run bundle && node scripts/generate-types.js && node scripts/dedupe-enums.js",
    "check": "npm run validate && npm run generate:types",
    "clean": "rm -rf types/generated-events.ts asyncapi-bundled.yaml"
  },
  "devDependencies": {
    "@asyncapi/cli": "^1.2.0",
    "@asyncapi/modelina": "^5.10.1",
    "@asyncapi/parser": "^3.0.0"
  }
}
